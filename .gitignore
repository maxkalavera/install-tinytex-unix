# ============================================================
# macOS
# ============================================================
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems
.apdisk
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.DocumentRevisions-V100
.Private
.com.apple.timemachine.donotpresent
*.icloud
*.m4v
*.m4a

# Xcode / macOS dev artifacts (optional, remove if unused)
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# ============================================================
# Nushell
# ============================================================
# Nushell config is usually kept in ~/.config, but if vendored:
.envrc
*.nu.history
history.txt
*.nuon.tmp

# Nushell plugin builds
target/
*.dylib
*.so
*.dll

# Local nu config overrides (if you commit defaults, ignore personal)
config.nu.local
env.nu.local

# ============================================================
# Amber
# ============================================================
# Amber compiled Bash output
*.sh
!*.sh.example
!scripts/*.sh
!docker/*.sh

# Amber build artifacts
build/
dist/
*.amberc
.amber/
amber.lock

# Amber compiler cache
.ambercache/

# Keep Amber sources
!*.ab

# ============================================================
# Bash
# ============================================================
# Shell history
.bash_history
.bash_profile.local
.bashrc.local
.bash_logout
.bash_sessions/

# Shellcheck / shfmt caches
.shellcheckrc.local
.shellcheck-cache/

# Bash scripts backups
*.sh.bak
*.bash.bak
*.orig
*~

# Local environment files
.env
.env.local
.env.*.local

# ============================================================
# Docker
# ============================================================
# Docker build context artifacts
.dockerignore.local
docker-compose.override.yml
docker-compose.*.local.yml

# Docker volumes / bind mount data (if local)
data/
volumes/
pgdata/
mysql-data/
redis-data/

# Docker build cache
.docker/
*.dockerfile.bak

# ============================================================
# Common / project-agnostic
# ============================================================
# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temp files
*.tmp
*.temp
*.swp
*.swo
*~
.~*

# Editor / IDE
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.iml
*.sublime-*
*.code-workspace
.vim/
.nvim/
tags
.TAGS

# Secrets / credentials
*.pem
*.key
*.crt
*.p12
secrets/
credentials.json
.env
.env.*

# CI / coverage
.coverage
coverage/
htmlcov/
.tox/
.nox/
.cache/

# Python (if Amber tooling or scripts use it)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (if any JS tooling)
node_modules/
.pnpm-store/
.yarn/
dist/
build/
.cache/

# OS misc
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/